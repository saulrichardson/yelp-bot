# Yelp for Business credentials
# - Optional if you run `npm run auth:biz` and rely on a persisted session.
# - Set only ONE credential pair (canonical OR alias).
YELP_BUSINESS_USERNAME=
YELP_BUSINESS_PASSWORD=

# Alias naming (supported):
# YELP_BIZ_USERNAME=
# YELP_BIZ_PASSWORD=

# Optional
PORT=3000
SLOW_MO_MS=250
ARTIFACTS_DIR=artifacts
YELP_BIZ_USER_DATA_DIR=state/yelp-biz/user-data

# This project always runs headful. If set to a truthy value, startup will throw.
# HEADLESS=false
